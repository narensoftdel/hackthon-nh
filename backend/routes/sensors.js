const express = require('express');
const router = express.Router();

// GET /api/sensors - returns dummy sensor data (handled in index.js for simplicity)
// Add more endpoints here as needed

module.exports = router;
